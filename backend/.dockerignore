# Without this, `docker build ./backend` uploads the entire local virtualenv
# (hundreds of MB) as build context, making every image build crawl.
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
.env
.env.local
.git/
